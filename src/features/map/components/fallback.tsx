const Fallback = () => {
  return (
    <div className="flex h-screen w-screen items-center justify-center bg-neutral-700 font-mono font-medium text-white">
      <h1>Loading map...</h1>
    </div>
  );
};

export default Fallback;
